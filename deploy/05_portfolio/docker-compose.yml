version: '3.9'

services:
  portfolio:
    image: ublcdnginx:latest
    container_name: ctlcdportfolio
    build:
      context: ../../
      dockerfile: deploy/05_portfolio/Dockerfile
    ports:
      - "1974:80"
      - "2222:22"
    networks:
      - red_portfolio
    restart: always

networks:
  red_portfolio:
    driver: bridge
